# Models

Place `.onnx` model files in this directory.

When a real model is added, keep a short contract note nearby with:

- input tensor names, shapes, layout, color order, normalization
- output tensor names, shapes, semantics
- required decode / threshold / NMS rules
